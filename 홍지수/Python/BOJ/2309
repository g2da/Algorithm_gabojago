import sys

arr = [int(sys.stdin.readline()) for i in range(9)]

for i in range(9):
    for j in range(i + 1, 9):
        if sum(arr) - arr[i] - arr[j] == 100:
            x, y = arr[i], arr[j]
            break
arr.remove(x)
arr.remove(y)=
for i in sorted(arr):
    print(i)
